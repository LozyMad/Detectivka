# Переменные окружения для проекта "Детективка"
# Скопируйте этот файл в .env и заполните нужными значениями

# Режим работы (development/production)
NODE_ENV=production

# Порт сервера
PORT=3000

# Секретный ключ для JWT токенов (обязательно измените в продакшене!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Тип базы данных: sqlite или postgresql
DB_TYPE=postgresql

# URL подключения к PostgreSQL (на хостинге задаётся в панели)
DATABASE_URL=postgresql://username:password@localhost:5432/detectivka

# Настройки базы данных (для SQLite)
DB_PATH=./database.sqlite

# Настройки CORS (для продакшена укажите ваш домен)
CORS_ORIGIN=*

# Секрет для вебхука автодеплоя (POST /api/deploy с заголовком X-Deploy-Secret)
# Если не задан, эндпоинт отключён
# DEPLOY_SECRET=your-random-secret
